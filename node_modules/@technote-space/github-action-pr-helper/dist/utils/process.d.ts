import { Octokit } from '@octokit/rest';
import { Logger } from '@technote-space/github-action-helper';
import { ActionContext } from '../types';
export declare const autoMerge: (pr: {
    created_at: string;
    number: number;
}, logger: Logger, octokit: Octokit, context: ActionContext) => Promise<boolean>;
export declare const execute: (octokit: Octokit, context: ActionContext) => Promise<void>;
