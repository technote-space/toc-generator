import type { ActionContext } from '../types';
import type { Types } from '@technote-space/github-action-helper';
import { Logger } from '@technote-space/github-action-log-helper';
export declare const autoMerge: (pr: {
    'created_at': string;
    number: number;
}, logger: Logger, octokit: Types.Octokit, context: ActionContext) => Promise<boolean>;
export declare const execute: (octokit: Types.Octokit, context: ActionContext) => Promise<void>;
